# For the memory section of the practice (--memory-backend mem0).
# The base lab and the graded assignment never need these; they run on the
# standard library alone and the file backend. Local configuration only: no
# account, no API key. Hardware-gated, never on the graded path.
#
#   ollama pull nomic-embed-text
#   uv run --with-requirements requirements-mem0.txt python3 context_lab.py <scenario> --memory-backend mem0
#
mem0ai>=2.0.20,<3
chromadb>=0.5.0
ollama>=0.3.0
