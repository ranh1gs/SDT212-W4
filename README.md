# Week 4: Context engineering

- **Practice (not graded):** [`practice/`](practice/README.md), assemble a model context from sourced parts, run it against a live model, watch a seeded context defect degrade the output, apply one assembly move, and compare the result and the token budget.
- **Assignment (graded):** [`assignment/`](assignment/README.md), on a held-out scenario, assemble the baseline context, diagnose the failure by category, apply one assembly move, re-run, and record what changed in `context-diagnosis.md`.

See the SDT 212 course information page in Canvas for tooling, provider options, disclosure, ownership, and submission rules.
# SDT208-W3
